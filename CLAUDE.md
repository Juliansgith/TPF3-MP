# CLAUDE.md

Always read [AGENTS.md](AGENTS.md) first and follow it. It holds the working
method for this repository: the branches (feature → `dev` → `acceptance` →
`main`), the checks each stage must pass before promotion, how releases
are made, and the rules every change follows. Never push to `acceptance` or
`main` except by the fast-forward promotions it describes.

@AGENTS.md
